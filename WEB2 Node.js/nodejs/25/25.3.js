function sum(num1, num2) {
  return num1 + num2;
}
console.log(`두 수의 합 : ${sum(4, 6)}`);

Math.round(1.6); //출력하려면 console.log로 해야함.
//filewrite("result.txt", Math.round(1.6)); //와 같은 형태로 보낼 수 있음.
//더 광범위하게 보낼 수 있음.
